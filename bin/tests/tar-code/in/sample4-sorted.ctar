-- === src/db/hello.sql ================================================
SELECT 'Hello from SQL 🌍!';

; === src/config/hello.ini ==============================================
; Hello from INI 🌍!

% === src/math/hello.m ================================================
disp('Hello from MATLAB 🌍!');

-- === src/scripts/hello.lua ============================================
print("Hello from Lua 🌍!")

#=== src/scripts/hello.pl ================================================
print "Hello from Perl 🌍!\n";

#=== src/scripts/hello.py ================================================
print("Hello from Python 🌍")

#=== src/scripts/hello.rb ================================================
puts "Hello from Ruby 🌍"

#=== src/scripts/hello.sh ================================================
#!/bin/bash
echo "Hello from Bash 🌍!"

// === src/lowlevel/hello.c ================================================
#include <stdio.h>
int main() {
    printf("Hello from C 🌍!\n");
    return 0;
}

// === src/lowlevel/hello.cpp ================================================
#include <iostream>
int main() {
    std::cout << "Hello from C++ 🌍!" << std::endl;
    return 0;
}

// === src/lowlevel/hello.cs ================================================
using System;
class Hello {
    static void Main() {
        Console.WriteLine("Hello from C# 🌍!");
    }
}

// === src/lowlevel/hello.go ================================================
package main
import "fmt"
func main() {
    fmt.Println("Hello from Go 🌍!")
}

// === src/lowlevel/hello.java ================================================
public class hello {
    public static void main(String[] args) {
        System.out.println("Hello from Java 🌍!");
    }
}

// === src/lowlevel/hello.kt ================================================
fun main() {
    println("Hello from Kotlin 🌍!")
}

// === src/lowlevel/hello.rs ================================================
fn main() {
    println!("Hello from Rust 🌍!");
}

// === src/web/hello.css =============================================== */
body {
  background: white;
  color: black;
}

/* === src/web/hello.less ============================================== */
@color: blue;
body { color: @color; }

/* === src/web/hello.pcss ============================================== */
body {
  font-family: sans-serif;
  color: purple;
}

/* === src/web/hello.sass ============================================== */
$color: red
body
  color: $color

/* === src/web/hello.scss ============================================== */
$color: green;
body {
  color: $color;
}

// === src/web/hello.js ================================================
console.log("Hello from JavaScript 🌍!");

// === src/web/hello.jsx ================================================
import React from 'react';
export default function App() {
  return <h1>Hello from JSX 🌍!</h1>;
}

// === src/web/hello.ts ================================================
let msg: string = "Hello from TypeScript 🌍!";
console.log(msg);

// === src/web/hello.tsx ================================================
import React from 'react';
export default function App(): JSX.Element {
  return <h1>Hello from TSX 🌍!</h1>;
}

<!-- === src/web/hello.html ============================================= -->
<!DOCTYPE html>
<html>
  <head><title>Hello HTML</title></head>
  <body>Hello from HTML 🌍!</body>
</html>

<!-- === src/web/hello.xml ============================================== -->
<message>Hello from XML 🌍!</message>
