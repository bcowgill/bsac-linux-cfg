this.that.theother.thing(r); // maybe not law of demeter violation
this.that(x).another(e).thing(f); // likely law of demeter violation
this.that(f).another(r).thing(a).ok(c).now(k).it(i).is(n).getting(g).ridiculous(r); // likely law of demeter violation
self.that.theother.thing(rr); // maybe not law of demeter violation
self.that(r).another(r).thing(r); // likely law of demeter violation
this.that(r).another(r); // likely law of demeter violation
this.method(r);  // not a law of demeter violation
this.value; // not a law of demeter violation
