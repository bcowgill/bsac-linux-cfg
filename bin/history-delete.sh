#!/bin/bash
echo To delete something from your bash command history:
echo "[SPACE] history | grep something"
echo "(the space prevents the command itself from being added to your history)"
echo Find the history number to delete and then:
echo [SPACE] history -d [NUM]
